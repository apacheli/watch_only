{
  "name": "watch_only",
  "version": "1.1.0",
  "description": "Redirects you to `/watch?v=` from `/shorts`",
  "main": "main.js",
  "scripts": {
    "x:build": "web-ext build --overwrite-dest",
    "x:lint": "web-ext lint"
  }
}
